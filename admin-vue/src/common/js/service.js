export default class Banner {
    constructor({id, imgSrc, activeState, title, sortOrder, createDate}) {
        this.id = id;
        this.imgSrc = imgSrc;
        this.activeState = activeState;
        this.title = title;
        this.sortOrder = sortOrder;
        this.createDate = createDate;
    }
}

// id
// imgSrc
// activeState
// sortOrder
// title
// createDate